spring:
 cloud:
  config:
   name: mysqlConfig
   profile: default
   lable: master
   discovery:
    serviceId: spring-cloud-configServer
    enabled: true
eureka:
 client:
  serviceUrl:
   defaultZone: http://localhost:7080/eureka